# Legacy pre-flow migrations

These files predate the `supabase/migrations/` timestamped flow. They were
applied to production by hand during the June–July 2026 build-out and are kept
for reference only — the applied database is authoritative for this era.
Everything from 2026-07-12 onward lives in `supabase/migrations/` and matches
the applied migration history exactly.

Do not re-run these.
